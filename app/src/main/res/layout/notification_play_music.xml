<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">


    <ImageView
        android:id="@+id/iv_music_poster"
        android:layout_width="@dimen/notify_music_poster_width"
        android:layout_height="@dimen/notify_music_poster_height"
        android:layout_centerVertical="true"
        android:src="@drawable/img1" />

    <LinearLayout
        android:id="@+id/ll_music_info"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_marginStart="10dp"
        android:layout_toEndOf="@+id/iv_music_poster"
        android:gravity="center_vertical"
        android:orientation="vertical">

        <TextView
            android:id="@+id/tv_music_name"
            style="@style/notify_music_name"
            android:text="歌曲名称" />

        <TextView
            android:id="@+id/tv_music_author"
            style="@style/notify_music_author"
            android:text="歌曲作者" />


    </LinearLayout>

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_alignParentEnd="true"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <ImageView
            android:id="@+id/iv_play_music_btn"
            style="@style/notify_music_play_btn"
            android:layout_marginEnd="15dp" />

        <ImageView
            android:id="@+id/iv_play_music_next"
            style="@style/notify_music_next_btn"
            android:layout_marginEnd="15dp" />

        <ImageView
            android:id="@+id/iv_play_music_close"
            style="@style/notify_music_close_btn"
            android:layout_marginEnd="15dp" />

    </LinearLayout>


</RelativeLayout>